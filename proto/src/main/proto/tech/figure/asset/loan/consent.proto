syntax = "proto3";

package tech.figure.asset.loan;


message Consent {
  string consent_type = 1; // see ConsentExpectedType in ExpectedEnumValues.kt
  OptStatus opt_status = 2;
  string credit_union = 3;
}

enum OptStatus {
  OPT_STATUS_UNKNOWN = 0;
  IN = 1;
  OUT = 2;
}